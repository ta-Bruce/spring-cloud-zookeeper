spring:
  application:
    name: "authorizationService"
  cloud:
    zookeeper:
      config:
        enabled: true
        root: configuration
        defaultContext: defaultConfiguration
        profileSeparator: ','
      discovery:
        enabled: true
        register: true
        root: "/services"